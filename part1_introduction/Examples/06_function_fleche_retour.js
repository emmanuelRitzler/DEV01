// const model = (x, y) => { return {x, y} } ;

const model = (x, y) => ({ x, y }) ;
console.log( model(1, 2) )

// 1. que retourne cette fonction

// 2. Une erreur se produit

// 3. la valeur de x uniquement

// 4. la valeur de y uniquement 

// 5. retourne l'objet { x : 1 , y : 2 }